export enum ConnectionState {
    connected = 1,
    connecting = 0,
    disconnected = 4,
    reconnecting = 2
}
export enum SignalrConnectionState {
    'Connected' = 1,
    'Connecting' = 0,
    'Disconnected' = 4,
    'Reconnecting' = 2
}