import { UpComingMarketsPipe } from './up-coming-markets.pipe';

describe('UpComingMarketsPipe', () => {
  it('create an instance', () => {
    const pipe = new UpComingMarketsPipe();
    expect(pipe).toBeTruthy();
  });
});
