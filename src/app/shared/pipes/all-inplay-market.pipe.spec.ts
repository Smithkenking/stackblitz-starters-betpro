import { AllInplayMarketPipe } from './all-inplay-market.pipe';

describe('AllInplayMarketPipe', () => {
  it('create an instance', () => {
    const pipe = new AllInplayMarketPipe();
    expect(pipe).toBeTruthy();
  });
});
