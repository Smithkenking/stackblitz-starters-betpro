import { Top8marketsPipe } from './top8markets.pipe';

describe('Top8marketsPipe', () => {
  it('create an instance', () => {
    const pipe = new Top8marketsPipe();
    expect(pipe).toBeTruthy();
  });
});
