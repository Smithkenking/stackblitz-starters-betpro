import { InplayMarketsPipe } from './inplay-markets.pipe';

describe('InplayMarketsPipe', () => {
  it('create an instance', () => {
    const pipe = new InplayMarketsPipe();
    expect(pipe).toBeTruthy();
  });
});
