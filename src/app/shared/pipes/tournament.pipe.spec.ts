import { TournamentPipe } from './tournament.pipe';

describe('TournamentPipe', () => {
  it('create an instance', () => {
    const pipe = new TournamentPipe();
    expect(pipe).toBeTruthy();
  });
});
