import { EventShortnamePipe } from './event-shortname.pipe';

describe('EventShortnamePipe', () => {
  it('create an instance', () => {
    const pipe = new EventShortnamePipe();
    expect(pipe).toBeTruthy();
  });
});
