import { RacingTimePipe } from './racing-time.pipe';

describe('RacingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RacingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
